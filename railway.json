{
  "build": {
    "builder": "MIXED",
    "buildCommand": "./mvnw clean package -DskipTests",
    "runCommand": "java -jar target/api-livros-*.jar"
  },
  "deploy": {
    "startCommand": "java -jar target/api-livros-*.jar",
    "healthcheckPath": "/actuator/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
